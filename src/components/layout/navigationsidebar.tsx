import React from 'react';
import { NavLink, Link } from 'react-router-dom';
import { Button } from '@/components/ui/button';
import { Home, Search, Library, Music2, ListMusic, Bell } from 'lucide-react';

interface NavItemProps {\n  to: string;\n  icon: React.ElementType;\n  label: string;\n}\n\nconst NavItem: React.FC<NavItemProps> = ({ to, icon: Icon, label }) => {\n  const navLinkClasses = ({ isActive }: { isActive: boolean }) =>\n    // Active state now uses semi-transparent bg-muted, consider a more opaque active state if needed for glassmorphism\n    `flex items-center gap-3 rounded-lg px-3 py-2 transition-all hover:text-primary ${\n      isActive ? 'bg-muted text-primary font-semibold backdrop-blur-sm' : 'text-muted-foreground hover:text-primary'\n    }`;\n\n  return (\n    <NavLink to={to} className={navLinkClasses}>\n      <Icon className=\"h-5 w-5\" />\n      {label}\n    </NavLink>\n  );\n};\n\nconst NavigationSidebar: React.FC = () => {\n  console.log('NavigationSidebar loaded');\n\n  return (\n    <aside className=\"fixed inset-y-0 left-0 z-10 hidden w-60 flex-col border-r bg-sidebar-background sm:flex pt-20 backdrop-blur-lg\"> {/* Use bg-sidebar-background (semi-transparent) and backdrop-blur-lg */}\n      <nav className=\"flex flex-col gap-2 px-4 py-4\">\n        <NavItem to=\"/\" icon={Home} label=\"Home\" />\n        <NavItem to=\"/search\" icon={Search} label=\"Search\" />\n        <NavItem to=\"/library\" icon={Library} label=\"Your Library\" />\n      </nav>\n      <div className=\"mt-auto p-4\">\n        {/* Placeholder for potential extra items or settings in sidebar footer */}\n        {/* Example: <Button variant=\"outline\" size=\"sm\" className=\"w-full\">Settings</Button> */}\n      </div>\n    </aside>\n  );\n};\n\nexport default NavigationSidebar;\n